import { create } from 'zustand'

export default create((set) => ({
    // game: null,
    // initGame: (
    //     playerNames: string[],
    //     houseRules?: {
    //         setup: (game) => void;
    //     }[]
    // ) => set(() => ({ game: new Game(playerNames, houseRules) })),
}));
